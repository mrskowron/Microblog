{% extends "layout.twig" %}

{% block pagetitle %} - Dodawanie wpisu{% endblock %}

{% block content %}

    <section id="content" class="register">

        <h2>Dodaj wpis</h2>
        
        <form novalidate class="form-horizontal" action="{{ url('insert') }}" method="post">
        
        {{ form_widget(form) }}
                
                <button type="submit" class="btn btn-success">Dodaj</button>

        </form>
    </section>
{% endblock %}

