{% extends "layout.twig" %}

{% block pagetitle %} - Logowanie{% endblock %}

{% block content %}

    <section id="content" class="register">

        <h2>Zaloguj się</h2>
        
        <form novalidate class="form-horizontal" action="{{ url('login') }}" method="post">
        
        {{ form_widget(form) }}
                
                <button type="submit" class="btn btn-success">Zaloguj się</button>

        </form>
    </section>
{% endblock %}

